{
     "builds": [
       {
         "src": "index.html",
         "use": "@vercel/static-build",
         "config": {
           "rollup": {
             "plugins": {
               "replace": {
                 "preprocess.env.process.env.API_BASE_URL": process.env.API_BASE_URL
               }
             },
             "external": [
               "react-toastify"
             ]
           }
         }
       }
     ]
   }
   